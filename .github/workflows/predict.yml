name: predict

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  predict:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install -r requirements.txt
        working-directory: nba-quant-system
      - name: Run prediction
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
          BALLDONTLIE_API_KEY: ${{ secrets.BALLDONTLIE_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python -m app.prediction_engine
        working-directory: nba-quant-system
      - name: Validate predictions saved
        run: |
          python -c "
          from app.database import get_conn, init_db
          from datetime import datetime
          import sys
          init_db()
          today = datetime.utcnow().strftime('%Y-%m-%d')
          with get_conn() as conn:
              count = conn.execute('SELECT COUNT(*) FROM predictions_snapshot WHERE snapshot_date=?', (today,)).fetchone()[0]
          print(f'预测已保存 {today}: {count}')
          if count == 0:
              print('错误: 今日无预测保存')
              sys.exit(1)
          print('预测验证成功')
          "
        working-directory: nba-quant-system
